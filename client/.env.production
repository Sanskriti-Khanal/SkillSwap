VITE_API_URL=https://YOUR-RENDER-APP.onrender.com/api

# Same backend, but origin only (no /api path) — used to build the
# production Content-Security-Policy connect-src (see vite.config.js).
# Keep this in sync with VITE_API_URL above.
VITE_API_ORIGIN=https://YOUR-RENDER-APP.onrender.com
